header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All tours

    .header__logo
        img(src='/img/logo-white.png' alt='Natours Photo')
    nav.nav.nav--user
        if user 
            a.nav__el(href='#') 
            a.nav__el(href='#')
                img.nav__user-img(src='/img/user.jpg' alt='User photo')
                span Ehsan

        else
            a.nav__el(href='/login') Log in
            a.nav__el(href='/signUp') Sign Up
            